## Zadig 中的构建变量透传 Demo

```
# 构建
$ make default
build the version
build done.

$ ./build/version.mac
Version(hard code): 0.1

$ ./build/version.mac version
Version: 
GitBranch:
CommitId:
Build Date: 
Go Version: 
OS/Arch: 
Build URL: 
```
